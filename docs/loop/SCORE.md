# Score vs the locked ten (2026-09-06, shallow)

| Axis | DocAgent | Best of the ten | Gap |
| --- | --- | --- | --- |
| GitHub first screen | Hero + matrix + convert shots | MinerU/Docling photos | Demo letter has **no image** |
| Agent API + capsule | Command + SHA-256×3 | none | none — keep |
| Same bytes (`prove`) | yes | none | none — keep |
| Images in engine | MD/HTML/PDF/DOCX/ODT | Pandoc/python-docx | Engine yes, **demo no** |
| Hangul | unique yes | none of the ten | **stop deepening** |

**This hour’s six (no trivia):**

1. landing — README first screen: “picture in, PDF/A out”. Do not invent OCR.
2. pdf — letter image must paint; no wrap-mode archaeology.
3. docx — picture roundtrip stays green; emit letter.docx when demo asks.
4. odt — same for ODT.
5. mdhtml — `![alt](docs/assets/mark.png)` relative path, not only data URIs.
6. demo — put the mark image in `examples/letter.md`, convert + prove, refresh `docs/assets/letter.*` and `output-*.png`.
