# DocAgent #1 loop state

Last cycle: 147 (2026-09-06) — Demo moved `letter-capsule.json` `output_hash` to `b468b37f1c3d8da7e4aac7e1a8417b46c29a57863817f9df16e8d4a6cc89f0f8` (input `b0f44237…`, plan `4b7360eb…`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` / `output-capsule.png` still show `b867db0a…` (pixels `b867db0aa9125feb…`) — shot lag, not a fake hash. prove stays distinct (`928b4745…` / `2607bb6f…`) — not copied onto capsule; first screen 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**; WeasyPrint tightness still **open**

## Public GitHub page

https://github.com/kevin9327/docagent

## Cycle 48 lanes

1. **landing** — Honest matrix vs the locked ten. Hangul images still **—**. No OCR or OmniDocBench numbers.
2. **pdf** — Section footer embeds in PDF/A; header-band `InlineObject::Image` layouts and paints as XObject.
3. **docx** — `WrapMode::Tight` round-trip (`wp:wrapTight`).
4. **odt** — `WrapMode::Behind` round-trip (`Gbehind`).
5. **mdhtml** — HTML `read` round-trips `<s>`/`<u>`/`<sup>`/`<sub>` and `<header>`/`<footer>`.
6. **hangul** — Nested Bullet/Number `NumberingRef.level` on HWP5+HWPX+HML+HWP3.

## Next

- First screen: 200px hero + unique-yes `<details>` + trio + hashes above vs-ten; Quickstart below. Hashes = capsule `b468b37f…`. Trio/capsule shots still `b867db0a…` (pixels `b867db0aa9125feb…`, lag — not a fake hash). prove JSON stays distinct (`928b4745…` / `2607bb6f…`) — do not copy onto capsule.
- Next gap: PDF page looking as dense as WeasyPrint/MinerU — table/paragraph tightness **open**, not claimed closed.
- Hangul images still **—**; **stop deepening Hangul**.

## Rule

Orchestrator fans out 6 lanes (LANES.md). One commit after clippy+tests. No overlapping files.

## Cycle 49 landing (2026-09-06) — historical

First screen sold **picture in, PDF/A out**. Hero / pipeline / ships dropped wrap-mode and Hangul trivia. Images row: PNG+JPEG **yes** on MD/HTML/PDF/A/DOCX/ODT, Hangul **—**. Demo letter landed `![Harbor mark](docs/assets/mark.png)`. Superseded: those convert shots now paint the teal mark; demo-image gap is **closed** (see Cycle 53). No OCR claims.

## Cycle 50 landing (2026-09-06) — historical

Shallow score vs the locked ten. `examples/letter.md` has `![Harbor mark](docs/assets/mark.png)`. Superseded: `output-pdf.png`, `output-html.png`, and `output-trio.png` now show the harbor mark under the H2; GitHub-image gap is **closed** (see Cycle 53). No new IR. No OCR. Hangul still **stop deepening**.

## Cycle 51 landing (2026-09-06)

GitHub first screen now uses the refreshed convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) that show the teal harbor mark under the H2. Dropped every stale / no-picture caption. Capsule hashes on README match `letter-capsule.json`. Capsule / determinism / GPU-free kept. Demo-image gap **closed**. Next gap: PDF page looking as dense as WeasyPrint/MinerU — still no invented numbers. No OCR. No wrap. Hangul still **stop deepening**.

## Cycle 52 (2026-09-06)

Committed finished lanes: DOCX `a:blip` + media PNG from `letter.md`; ODT `draw:image` + Pictures/ PNG; demo capsule/prove/`letter.pdf`/`output-*.png` after the 24pt mark scale. Next: landing lockstep README hashes with capsule; PDF page density vs WeasyPrint/MinerU; demo keep assets in lockstep. docx/odt/mdhtml: do not invent work. No OCR. Hangul still **stop deepening**.

## Cycle 53 landing (2026-09-06)

First screen uses the harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`. Dropped stale / no-picture captions and the concept-art JPGs (`convert-story.jpg`, `capsule-page.jpg`) that are not engine output. README capsule hashes lockstep with `letter-capsule.json` (`output_hash` `f09d223f56e8adec474d9c24c3f0b2667f72104b1f67465f5e63c6c997a19ddf`). Capsule / determinism / GPU-free kept. Demo-image gap **closed**. Cycles 49–50 “text-only / gap open” captions are historical. Next gap: PDF page density vs WeasyPrint/MinerU. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 54 (2026-09-06)

Committed PDF letter-page density (type, spacing, table) vs WeasyPrint/MinerU plus landing captions/hashes. Demo must refresh `letter.*` / `output-*.png` so GitHub matches the denser page. docx/odt/mdhtml: do not invent work. No OCR. Hangul still **stop deepening**.

## Cycle 55 landing (2026-09-06)

First screen uses the harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`. Captions already show the teal mark under the H2; no text-only / gap-open language; concept-art JPGs stay unused. After density commit `e93747f`, README and `docs/src` hashes lockstep with `letter-capsule.json` (`output_hash` `e38bb197cbc8a9379834e71c2a4827d5d8a5cc70af67ef2a68501fceccce916a`; input and plan unchanged). Capsule / determinism / GPU-free kept. Demo-image gap **closed**. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 56 (2026-09-06)

Committed denser-page `letter.pdf` / `output-*.png` plus README hash lockstep (`output_hash` `e38bb197…`). Next gap still PDF page density vs WeasyPrint/MinerU — no invented numbers. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 57 landing (2026-09-06)

Master `5d51779` already locksteps README / `docs/src` / `competitors.json` with `letter-capsule.json` (`output_hash` `e38bb197cbc8a9379834e71c2a4827d5d8a5cc70af67ef2a68501fceccce916a`). First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2; concept-art JPGs stay unused. No README churn. Capsule / determinism / GPU-free kept. Demo-image gap **closed**. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 58 score (2026-09-06)

Hourly score rewritten. Demo-image gap **closed**. Remaining gap: WeasyPrint table/paragraph tightness (heading density already locked). prove JSON still looks stale vs capsule. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 59 landing (2026-09-06)

Hashes already lockstep with `letter-capsule.json` after demo re-ran `prove` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `e38bb197cbc8a9379834e71c2a4827d5d8a5cc70af67ef2a68501fceccce916a`). README / `docs/src` / `competitors.json` already match; no hash churn. First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2. Demo-image gap **closed**. Do not claim the WeasyPrint table/paragraph tightness gap is closed. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 60 (2026-09-06)

Committed WeasyPrint table cell padding and paragraph gap after the mark (layout/paint tests). Demo must refresh `letter.pdf` / `output-*.png` / capsule after this layout change. Do not claim the WeasyPrint gap is closed. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 61 landing (2026-09-06)

Demo refreshed `letter-capsule.json` after tightness commit `288c7ba`. README / `docs/src` / `competitors.json` lockstep to that capsule (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b4b3843b18793d3fa50e43706583fc7bd388f83fef04ecabaccb2cb6cf084fc1`). First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 62 (2026-09-06)

Committed tightness-pass `letter.pdf` / `output-*.png` / capsule (`output_hash` `b4b3843b…`) and README lockstep. WeasyPrint gap still **open**. prove `plan_hash` still `928b4745…` vs capsule `4b7360eb…` after a real prove run — do not fake hashes. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 63 landing (2026-09-06)

Master `2455a82` already locksteps README / `docs/src` / `competitors.json` with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b4b3843b18793d3fa50e43706583fc7bd388f83fef04ecabaccb2cb6cf084fc1`). First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2. No hash or caption churn. Capsule / determinism / GPU-free kept. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 64 (2026-09-06)

No engine diffs. Landing/pdf/demo verified lockstep after `2455a82`. WeasyPrint tightness still the open gap. prove vs capsule `plan_hash` split remains after a real prove run — do not fake hashes. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 65 landing (2026-09-06)

Master `0c4b43e` / `2455a82` already locksteps README / `docs/src` / `competitors.json` with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b4b3843b18793d3fa50e43706583fc7bd388f83fef04ecabaccb2cb6cf084fc1`). First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2. No hash or caption churn. Capsule / determinism / GPU-free kept. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 66 (2026-09-06)

No engine diffs. Landing/pdf/demo verified again. WeasyPrint tightness still open. prove vs capsule `plan_hash` split remains — do not fake hashes. Next hour should rewrite SCORE. docx/odt/mdhtml: do not invent work. Hangul still **stop deepening**.

## Cycle 67 landing (2026-09-06)

Master `d8bfe2c` / `2455a82` already locksteps README / `docs/src` / `competitors.json` with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b4b3843b18793d3fa50e43706583fc7bd388f83fef04ecabaccb2cb6cf084fc1`). First screen is harbor-mark convert shots (`output-trio.png`, `output-pdf.png`, `output-html.png`) plus `mark.png`; captions already say teal under H2. No hash or caption churn. Capsule / determinism / GPU-free kept. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 68 landing (2026-09-06)

GitHub first screen packed to MinerU/Docling density for an **agent runtime**: shorter hero, more badges, `<details>` capability summary, Quickstart (`convert` + `prove` + `Engine` snippet) and capsule hashes **above** the vs-the-ten matrix, then one `output-trio.png` (teal mark under H2). PDF/HTML/capsule shots and mark fixture moved below the fold. Hero / pipeline / ships SVGs now carry C/Q/E, hash prefixes from `letter-capsule.json`, and an explicit “WeasyPrint tightness not claimed”. README / `docs/src` / `competitors.json` hashes lockstep (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b4b3843b18793d3fa50e43706583fc7bd388f83fef04ecabaccb2cb6cf084fc1`). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 69 landing (2026-09-06)

Demo refreshed `letter-capsule.json` / `output-*.png` after sibling engine diffs. README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `203bc5a4d73b9293f0d82b06da651af3d5da410d71fadef79cb4b8bb21edec76`). First screen stays Quickstart + hashes + vs-ten + `output-trio.png`; captions still teal mark under H2 (Northwind Freight letter). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 70 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `7f8a9c781d2b9723f0b73e49426f0fef74f778dc233b08623e560e34f1731c58` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. First screen stays Quickstart + hashes + vs-ten + `output-trio.png`; captions still teal mark under H2 (Northwind Freight letter). Convert shots still show the mark; `output-capsule.png` / trio capsule panel may lag until demo refreshes PNGs — do not fake shot hashes. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 71 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `c26d7bd08c972d1c80ac490fc6ae5c353436f8e2b22fef7e169ebe9173ef2711` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `c26d7bd0…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 72 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `c26d7bd08c972d1c80ac490fc6ae5c353436f8e2b22fef7e169ebe9173ef2711`). Captions match current `output-*.png` (teal mark under H2; trio/capsule panel `c26d7bd0…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 73 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `8e04175dfa66d0c35ad4b1924c58d5b8737c651d17a71024d75a3134729585b7` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `8e04175d…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 74 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `8e04175dfa66d0c35ad4b1924c58d5b8737c651d17a71024d75a3134729585b7`). Captions match current `output-*.png` (teal mark under H2; trio/capsule panel `8e04175d…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 75 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `f4376eeed793e12d8bb4b923f24c553fa9e8e9a0a5fb4b3de8cfcd8f9689b67b` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `f4376eee…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 76 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `f4376eeed793e12d8bb4b923f24c553fa9e8e9a0a5fb4b3de8cfcd8f9689b67b`). Captions match current `output-*.png` (teal mark under H2; trio/capsule panel `f4376eee…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 77 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `f4376eeed793e12d8bb4b923f24c553fa9e8e9a0a5fb4b3de8cfcd8f9689b67b`). Captions match current `output-*.png` (teal mark under H2; trio/capsule panel `f4376eee…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 78 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `df1adb119e5889f399a978a949fabbe6bce09c875cb32608bfb90c4e0f2020bb` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `df1adb11…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 79 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `6f8185769d68652c4de884eb258ce8bd23356d0dc83bdf7953181ee39fee9373` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `6f818576…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 80 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `6f8185769d68652c4de884eb258ce8bd23356d0dc83bdf7953181ee39fee9373`). Captions match current `output-*.png` (teal mark under H2; trio/capsule panel `6f818576…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 81 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `4346187d22662f0abc8e45f3a542f0b6c755c8901b56b603a71216dffdc2a125` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. First screen stays Quickstart + hashes + vs-ten + `output-trio.png`; captions still teal mark under H2 (Northwind Freight letter). Convert shots still show the mark; `output-capsule.png` / trio capsule panel still show `d71c984f…` until demo refreshes PNGs — do not fake shot hashes. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 82 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `77f65b2907e3bc89edf307b807339f048fee028a72924fa89d1131691dfef8b5` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `77f65b29…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 83 landing (2026-09-06)

Demo moved `letter-capsule.json` and `output-*.png` after sibling engine diffs. Capsule `output_hash` is now `b6edba2fa00ee1d4739412cd0890b87edf3d41f62d3fb5f944752c66564cc49c` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `b6edba2f…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 84 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `00158ebb2bd076222c4dd997d317997564aab009de1cef8fe484632dbd85a700` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline SVGs lockstep to that capsule. First screen stays Quickstart + hashes + vs-ten + `output-trio.png`; captions still teal mark under H2 (Northwind Freight letter). Convert shots still show the mark; `output-capsule.png` / trio capsule panel still show `b6edba2f…` until demo refreshes PNGs — do not fake shot hashes. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 85 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `00158ebb2bd076222c4dd997d317997564aab009de1cef8fe484632dbd85a700`). Demo refreshed `output-trio.png` / `output-capsule.png`: both now show `00158ebb…` — captions updated; no shot-lag. First screen stays Quickstart + hashes + vs-ten + trio (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 86 landing (2026-09-06)

MinerU/Docling density: hero SVG cut 420→240; convert trio + capsule hashes sit **above** the vs-the-ten matrix (Docling-style picture on the first screen). PDF/HTML shots are a two-up below the fold; capsule shot still `00158ebb…`. README / `docs/src` / `competitors.json` / hero SVG lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `00158ebb2bd076222c4dd997d317997564aab009de1cef8fe484632dbd85a700`). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open** — not claimed closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 87 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `d8f2357a92f27c2559ff1e035f71015d4082d00c5142202480c451a453dd576f` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `d8f2357a…` — captions updated; no shot-lag. First screen stays short hero (240) + trio **above** vs-ten (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 88 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `d8f2357a92f27c2559ff1e035f71015d4082d00c5142202480c451a453dd576f`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `d8f2357a…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 89 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `7c9f05e480469709497222d4ebcf0ee3604cb5c0512b1836f95d324eb03d1bc5` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `7c9f05e4…` — captions updated; no shot-lag. First screen stays short hero (240) + trio **above** vs-ten (teal mark under H2, Northwind Freight). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 90 landing (2026-09-06)

MinerU/Docling density: hero SVG 240→200; convert trio + capsule hashes sit **above** vs-the-ten; Quickstart (`convert` + `prove` + `Engine`) sits **below** the matrix (MinerU features-before-install). One-line runtime strip under badges (`Command::Convert` · SHA-256 ×3 · i32 1/7200" · PNG/JPEG → PDF/A XObject). README / `docs/src` / `competitors.json` / hero+ships SVGs lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `7c9f05e480469709497222d4ebcf0ee3604cb5c0512b1836f95d324eb03d1bc5`). Trio/capsule shots still `7c9f05e4…` — no shot-lag, no fake hashes. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open** — not claimed closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 91 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `293f79819c5d864ed9d16d380ffce4417ae36362429946a497d3ff821f7c9ff3` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `293f7981…` — captions updated; no shot-lag. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 92 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `293f79819c5d864ed9d16d380ffce4417ae36362429946a497d3ff821f7c9ff3`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `293f7981…`). No README churn. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 93 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `3616f859b5bddf6e2b7d68324125207b7b4d68252c2563486f15570d9a2966db` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Convert shots still show the mark; `output-capsule.png` / trio capsule panel still show `293f7981…` until demo refreshes PNGs — do not fake shot hashes. Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 94 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `3616f859b5bddf6e2b7d68324125207b7b4d68252c2563486f15570d9a2966db`). Captions already honest: teal mark under H2; JSON `3616f859…`; trio/capsule panel still `293f7981…` (shots lag — not a fake hash). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 95 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `3616f859b5bddf6e2b7d68324125207b7b4d68252c2563486f15570d9a2966db`). Captions already honest: teal mark under H2; JSON `3616f859…`; trio/capsule panel still `293f7981…` (shots lag — not a fake hash). `intro.md` now names that lag next to the hash table. No README restyle. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 96 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `b602e8d4…` — captions updated; no shot-lag. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 97 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 98 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` / `output-pdf.png` / `output-html.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 99 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` / `output-pdf.png` / `output-html.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 100 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` / `output-pdf.png` / `output-html.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 101 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` / `output-pdf.png` / `output-html.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 102 landing (2026-09-06)

MinerU-density pass: collapsed unique-yes `<details>` (C/Q/E · capsule SHA-256 ×3 · `prove` — none of the locked ten; WeasyPrint tightness **not** claimed) sits under badges, then harbor-mark trio + full capsule SHA-256 ×3 **above** vs-the-ten, Quickstart **below**. README / `docs/src` / `competitors.json` / ships.svg lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Trio/capsule shots show `b602e8d4…` — no shot-lag. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 103 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b602e8d40b62af4aca84c443bc70bff178f8fc2ce9626b2b3afb36c83fd6cdfd`). Captions match current `output-trio.png` / `output-capsule.png` / `output-pdf.png` / `output-html.png` (teal mark under H2; panel `b602e8d4…`). No README churn. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**. Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 104 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `4f98b1204084cfe52c6d3e27167fc705747a303cad1da0e68a7ff7bff8000f89` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `4f98b120…` — captions updated; no shot-lag. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 105 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `b9f69c7f8a5364c597b5f9a99acd7b736f431c718faf2c1b1dacbf4db888fbb2` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `b9f69c7f…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `1ffd46c8792d7ca9bf4f031759467643d53bd500ac711ebe0ac590013042c082`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 106 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b9f69c7f8a5364c597b5f9a99acd7b736f431c718faf2c1b1dacbf4db888fbb2`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `b9f69c7f…`). prove stays **distinct** (`plan` `928b4745…`, `output` `1ffd46c8792d7ca9bf4f031759467643d53bd500ac711ebe0ac590013042c082`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 107 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `ca738943108768080e6e7eb8de5e262aa0fda022672acf9efbe8fed9f9a9b58b` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `ca738943…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `a446b35fc5e2fafa1a71b66facd1840b1bc485dc0eadf0492cd2b2bf3c385ce5`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 108 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `2b6ac2100bc927bd74fc85bce0cec317dd4a8d4c271fc84e183a7cbd5677c541` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `2b6ac210…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `a446b35fc5e2fafa1a71b66facd1840b1bc485dc0eadf0492cd2b2bf3c385ce5`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 109 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `99cd26216aafdfec68890f9830cbe17394fb67819e3577a9cf881e2b77e52ec7` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `99cd2621…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `1b127f375dc554299c6805b122f4967fb63554555724e4d85056bfa3a556555b`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 110 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `99cd26216aafdfec68890f9830cbe17394fb67819e3577a9cf881e2b77e52ec7`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `99cd2621…`). prove stays **distinct** (`plan` `928b4745…`, `output` `1b127f375dc554299c6805b122f4967fb63554555724e4d85056bfa3a556555b`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 111 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `1a8b11388ca3e20f46073acbd6f78961d0b1ad4853d17066618687c6a9e069eb` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `1a8b1138…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `57a5cfd43dbd42785416a655c368b39f6823befcd5989e46f12f4c2eae0c9624`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 112 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `1a8b11388ca3e20f46073acbd6f78961d0b1ad4853d17066618687c6a9e069eb`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `1a8b1138…`). prove stays **distinct** (`plan` `928b4745…`, `output` `57a5cfd43dbd42785416a655c368b39f6823befcd5989e46f12f4c2eae0c9624`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 113 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `1a8b11388ca3e20f46073acbd6f78961d0b1ad4853d17066618687c6a9e069eb`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `1a8b1138…` — pixels read as `1a8b1138ca3e20f4…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `57a5cfd43dbd42785416a655c368b39f6823befcd5989e46f12f4c2eae0c9624`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 114 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `98d06a96cfe339e6dc4b9e34299b25845b121e0c9c4dec56f69cb62597574f48` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `98d06a96…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `d8974649d47b2b4125b6e90f9f357185d77d493f538cbcf5fb88dde437f41d66`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 115 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `64bb7816657f0a5f05b438f94d282bb053810c8ec10bb0e5d4552cdc6286a412` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `64bb7816…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `e156946ad699424db873656ff275c89d367e68cd4df8836e7b3840c69c5d2eab`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 116 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `64bb7816657f0a5f05b438f94d282bb053810c8ec10bb0e5d4552cdc6286a412`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `64bb7816…` — pixels read as `64bb7816657f0a5f…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `e156946ad699424db873656ff275c89d367e68cd4df8836e7b3840c69c5d2eab`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 117 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `21c3497bb6bd713c38e6da0ba8b57857b0924c2c4e9d0474c70038d035c66396` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Convert shots still show the mark; `output-capsule.png` / trio capsule panel still show `e5911f94…` until demo refreshes PNGs — do not fake shot hashes. prove stays **distinct** (`plan` `928b4745…`, `output` `73ff01d7ac88dc45dc1ba487384b8313ebc9e2d43b0a444941334762b1f5cac2`) — do not copy onto capsule. No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 118 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `9ea6c1060a9a4a9349d61085a89aa37569b8e219c8e3931ac6c5e11e64a681a2` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `9ea6c106…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `22c5a46ce249bffed8fb6e434b51b0dc88d5e410518deab2b65c3c047e7e19c1`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 119 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `d12c9ad6db7695461b1c8ffcb6dfd9fb8832d2530be66993e50df78edae3724f` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `d12c9ad6…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `1983193943918c7e0c4fb7658712e1c4fe16af26056fc6f6572b22c9bd9f20d3`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 120 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `4c081cb6d7b50970bf4b52192f2037d4b860727786bf9c29c770608da14a9bce` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `4c081cb6…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `1983193943918c7e0c4fb7658712e1c4fe16af26056fc6f6572b22c9bd9f20d3`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 121 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `4c081cb6d7b50970bf4b52192f2037d4b860727786bf9c29c770608da14a9bce`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `4c081cb6…` — pixels read as `4c081cb6d7b50970…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `1983193943918c7e0c4fb7658712e1c4fe16af26056fc6f6572b22c9bd9f20d3`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 122 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `8d4b7dda4cb7b1efa952f4e17d2f2c45a4a169761f8a087583a933d1638ae5e4` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `8d4b7dda…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `6fdb948c3b132df834cd7c90f7fc45f3b90662880e97a07aea30f7e0db07e215`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 123 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `8d4b7dda4cb7b1efa952f4e17d2f2c45a4a169761f8a087583a933d1638ae5e4`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `8d4b7dda…` — pixels read as `8d4b7dda4cb7b1ef…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `6fdb948c3b132df834cd7c90f7fc45f3b90662880e97a07aea30f7e0db07e215`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 124 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `7fe432942648d55f01de4bb004a4968eea33f909d9b9a19a595332fd9a60e8ed` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `7fe43294…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `6fdb948c3b132df834cd7c90f7fc45f3b90662880e97a07aea30f7e0db07e215`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 125 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `39bec5f15ef102319397935d77d5ea4a58515cd2b52042946638861859eca5e3` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `39bec5f1…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `3af5d771c89cbb77715a762bedc3691dd42d5644d42a6722a2014d55da8debc0`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 126 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `6624eb0eaf7ad29e51958304b716c7ea462de6d926c150bdc0b04d8fc8605946` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `6624eb0e…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `cd2323695fc71910b2ddae5ffb89114776ec598e1b59c92ee77f2a98da47de86`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 127 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `6624eb0eaf7ad29e51958304b716c7ea462de6d926c150bdc0b04d8fc8605946`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `6624eb0e…` — pixels read as `6624eb0eaf7ad29e…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `cd2323695fc71910b2ddae5ffb89114776ec598e1b59c92ee77f2a98da47de86`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 128 landing (2026-09-06)

Hashes stay lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `6624eb0eaf7ad29e51958304b716c7ea462de6d926c150bdc0b04d8fc8605946`). Trio/capsule shots still `6624eb0e…` (pixels `6624eb0eaf7ad29e…`) — no lag. prove stays **distinct** (`plan` `928b4745…`, `output` `cd232369…`) — not copied onto capsule. First screen stays MinerU-density: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**. `docs/src` (intro, capabilities, codec-guide, format-hops) refreshed to match README (`convert` + `--docx` + `Engine` + C/Q/E + images-first row + prove-distinct). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 129 landing (2026-09-06)

Demo refreshed `letter-capsule.json` and `output-*.png`. Capsule `output_hash` is now `19d1e6049b0bac8cb7c452e97822c26deeadf70e2df7d3b1947249a2e63e7ce1` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `19d1e604…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `8d86d817f8a3416aa33a0f9c31b6f0119b07150fe301c5225325c2ef89f9d2cd`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 130 landing (2026-09-06)

Hashes stay lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `19d1e6049b0bac8cb7c452e97822c26deeadf70e2df7d3b1947249a2e63e7ce1`). Trio/capsule shots still `19d1e604…` — no lag. prove stays **distinct** (`928b4745…` / `8d86d817…`) — now named on README Quickstart and `docs/src` (intro, capabilities, codec-guide, format-hops); not copied onto the capsule table. First screen stays MinerU-density: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below** (teal harbor mark under the H2). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 131 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `3c7982bc40fcbac4d84298416941b86fcafd3600dcb90631c750317f36ef837b` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `3c7982bc…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2767f36165d0b2a1a517b6ed962fd2c820e44d8a07949b40a802fe402d2dfd57`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 132 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `3c7982bc40fcbac4d84298416941b86fcafd3600dcb90631c750317f36ef837b`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `3c7982bc…` — pixels read as `3c7982bc40fcbac4…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2767f36165d0b2a1a517b6ed962fd2c820e44d8a07949b40a802fe402d2dfd57`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 133 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `757818db55b0d70c1c612386ded47eaa4fd672c8f5f2a71cf559b54ad4ba9890` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `757818db…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2767f361…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 134 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `757818db55b0d70c1c612386ded47eaa4fd672c8f5f2a71cf559b54ad4ba9890`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `757818db…` — pixels read as `757818db55b0d70c…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2767f36165d0b2a1a517b6ed962fd2c820e44d8a07949b40a802fe402d2dfd57`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 135 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `5e94d3c3b3298d5020f26688f321a052ca1bf1130da99369fb28598c4806fefc` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `5e94d3c3…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2767f361…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 136 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `5e94d3c3b3298d5020f26688f321a052ca1bf1130da99369fb28598c4806fefc`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `5e94d3c3…` — pixels read as `5e94d3c3b3298d50…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2767f36165d0b2a1a517b6ed962fd2c820e44d8a07949b40a802fe402d2dfd57`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 137 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `4d2c757b3e8667c09a37cd627225f1b5d575511129d15a4fb2c16e42cd794a20` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `4d2c757b…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2767f361…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 138 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `06036fca58dcc8a7770984bb87203c5143d9ebcbaf5d96fd2ece543076dac8ef` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `06036fca…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). `intro.md` puts trio + hashes **above** Quickstart. No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 139 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `7c8c122198025d59a45b131dc732c2eb86a99a1ccc7dbd77d9da0cbae3141375` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `7c8c1221…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 140 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `7c8c122198025d59a45b131dc732c2eb86a99a1ccc7dbd77d9da0cbae3141375`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `7c8c1221…` — pixels read as `7c8c122198025d59…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2607bb6f3b83f537cc4883574ed18d6da820fe1d3bd8c6863d1e87be8fa3b403`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 141 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `4a48ccd69e7b08f739bd39c12a175bec662335e9e283d391d6c214419fbca231` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `4a48ccd6…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 142 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `7be7c54f5d3aac21fd77b6578e8a615a1113cd76fa64c5187be8daefabe197fa` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `7be7c54f…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 143 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `7be7c54f5d3aac21fd77b6578e8a615a1113cd76fa64c5187be8daefabe197fa`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `7be7c54f…` — pixels read as `7be7c54f5d3aac21…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2607bb6f3b83f537cc4883574ed18d6da820fe1d3bd8c6863d1e87be8fa3b403`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 144 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `7be7c54f5d3aac21fd77b6578e8a615a1113cd76fa64c5187be8daefabe197fa`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `7be7c54f…` — pixels read as `7be7c54f5d3aac21…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2607bb6f3b83f537cc4883574ed18d6da820fe1d3bd8c6863d1e87be8fa3b403`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 145 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `b867db0aa9125febfa9d28b7836292c2c03a53f7cc10f96cdd6c2d04d40bb4e7` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. `output-trio.png` and `output-capsule.png` show `b867db0a…` — captions updated; no shot-lag. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 146 landing (2026-09-06)

Verified: README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs already lockstep with `letter-capsule.json` (`input` `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, `plan` `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`, `output` `b867db0aa9125febfa9d28b7836292c2c03a53f7cc10f96cdd6c2d04d40bb4e7`). Captions match current `output-trio.png` / `output-capsule.png` (teal mark under H2; panel `b867db0a…` — pixels read as `b867db0aa9125feb…`). prove stays **distinct** (`plan` `928b47459b089d2dc7e60d6f764b798aaaa955a92ba6977ea6c157e7d88eea63`, `output` `2607bb6f3b83f537cc4883574ed18d6da820fe1d3bd8c6863d1e87be8fa3b403`) — not copied onto capsule. No README restyle (MinerU density already matches: 200px hero + unique-yes `<details>` + trio + hashes **above** vs-ten, Quickstart **below**). Demo-image gap **closed**. WeasyPrint table/paragraph tightness still **open**. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.

## Cycle 147 landing (2026-09-06)

Demo moved `letter-capsule.json` `output_hash` to `b468b37f1c3d8da7e4aac7e1a8417b46c29a57863817f9df16e8d4a6cc89f0f8` (input `b0f4423728db4238779b3bf6cbd238d4d0d637f733c078322be6ff23398bbcdf`, plan `4b7360eb1b7a9bec20cc35d5c79f2156c22d2712ab62ededfb60e328b8f16d3a`). README / `docs/src` / `competitors.json` / hero+pipeline+ships SVGs lockstep to that capsule. First screen stays 200px hero + unique-yes `<details>` + trio **above** vs-ten, Quickstart **below** (teal mark under H2, Northwind Freight). Convert shots still show the mark; `output-capsule.png` / trio capsule panel still show `b867db0a…` (pixels `b867db0aa9125feb…`) until demo refreshes PNGs — do not fake shot hashes. prove stays **distinct** (`plan` `928b4745…`, `output` `2607bb6f…`) — do not copy onto capsule. No README restyle (MinerU density already matches). Demo-image gap **closed**. Do **not** claim the WeasyPrint table/paragraph tightness gap is closed. SCORE rewritten vs the locked ten. Capsule / determinism / GPU-free kept. No OCR. No OmniDocBench numbers. Hangul still **stop deepening**.


