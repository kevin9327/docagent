# DocHWP

Deterministic flow-document runtime for agents. No GUI.

```
cargo test --workspace
cargo run -p dochwp-cli -- convert fixtures/example.hwp --pdf out.pdf --html out.html --ir out.json
```

See `AGENTS.md` and `docs/`.
